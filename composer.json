{
  "require": {
    "phpmyadmin/phpmyadmin": "^5.2",
    "ext-pdo_mysql": "*",
    "php": ">=7.0",
    "vlucas/phpdotenv": "^3.6",
    "monolog/monolog": "^2.0"
  },
  "config": {
    "vendor-dir": "vendor"
  },
  "scripts": {
    "post-install-cmd": [
      "echo 'Dependencies installed successfully!'"
    ]
  }
}
